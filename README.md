# AI Knowledge

个人 AI 知识库。

## 目录规则

每个主题使用一个独立目录，并直接放在仓库根目录，与 `AGENTS.md` 同层。

```text
ai-knowledge/
├── AGENTS.md
├── README.md
├── rag/
├── agent-memory/
└── gui-agent/
```

不使用 `topics/` 汇总目录。所有主题统一在 Git `main` 分支维护。
